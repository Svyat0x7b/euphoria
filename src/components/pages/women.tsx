import { Filters, ProductList } from '../women';

const Women = () => {
    return (
        <main className="px-[100px] flex">
            <Filters />
            <ProductList />
        </main>
    );
};

export default Women;
